# Custom Character

Put your custom player sprite here as:

- `player.png`

Optional alternatives:

- `player.webp`
- `player.jpg`

The game checks this folder first when you pick `Custom` in the character selector.
